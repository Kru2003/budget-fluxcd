apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "budget-chart.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "budget-chart.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.statefulset.env | nindent 2 }}
