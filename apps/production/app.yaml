apiVersion: v1
kind: Service
metadata:
  name: budget-app-svc
  namespace: budget
spec:
  selector:
    app: budget
  ports:
    - port: 80
      name: app-svc-port
      targetPort: app-port
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-app
  namespace: budget
  labels:
    app: budget
spec:
  replicas: 1
  selector:
    matchLabels:
      app: budget
  template:
    metadata:
      labels:
        app: budget
    spec:
      containers:
        - name: budget-app
          image: krupanshi20/budget:main-c41eefa-1750657926@sha256:34f2fc76182adb4bfd5083fb8daa823d4701acd0691b28e47a06b7151c0f8e68 # {"$imagepolicy": "flux-system:budget-policy"}
          ports:
            - containerPort: 80
              name: app-port
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: database-env
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: database-env
                  key: DB_PORT
